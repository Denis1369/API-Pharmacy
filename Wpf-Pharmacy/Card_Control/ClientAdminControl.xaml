<UserControl x:Class="Wpf_Pharmacy.Card_Control.ClientAdminControl"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="500">
    <Border Background="White" Padding="12" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Top">
                <TextBlock Text="{Binding ClientEmail}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                <StackPanel Grid.Column="0" VerticalAlignment="Top">
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                        <TextBlock Text="{Binding ClientLastName}" Foreground="#666" FontSize="12"/>
                        <TextBlock Text="{Binding ClientName}" Foreground="#666" FontSize="12" Margin="4,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>

            <!-- Статус -->
            <StackPanel Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Right">
                <ComboBox x:Name="StatusComboBox"
                          SelectedItem="{Binding ClientStatus, Mode=TwoWay}"
                          Width="120"
                          DropDownClosed="StatusComboBox_DropDownClosed">
                </ComboBox>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>